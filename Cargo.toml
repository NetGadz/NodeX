[workspace]
members = [
    "core-ffi",
    "nodex-kademlia",
    "nodex-messenger",
    "nodex-cli",
    "nodex-gui",
]
resolver = "2"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = "symbols"
debug = false