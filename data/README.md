# NodeX Local Data Directory

This directory is used for local data storage, database files, and caches during development.
In production, user databases and state files are safely isolated in standard OS application directories:
- **Windows**: `%APPDATA%\NodeX\`
- **Linux/macOS**: `~/.config/nodex/`
